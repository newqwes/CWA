import { connect } from 'react-redux';
import GitHelperComponent from '../components/GitHelperComponent';

const mapStateToProps = () => ({});

export default connect(mapStateToProps)(GitHelperComponent);
